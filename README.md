### 🧠 Архитектура и принцип работы CPN

CPN состоит из двух логических частей:

1. **Слой Кохонена (SOM — Self-Organizing Map)**
   Это **неподконтрольный слой**, который самоорганизуется и находит структуру данных.
   Он преобразует входные векторы в “нейроны-победители” — каждый вход проецируется на ближайший по весам нейрон решётки.
   Этот слой отвечает за **кластеризацию** и **топологическое представление данных**.

2. **Слой Гроссберга (Grossberg Layer)**
   Это **подконтрольный (supervised) слой**, который учится ассоциировать выход SOM (индекс нейрона-победителя) с правильной меткой класса.
   Он использует **дельта-обучение с усреднением ошибок (Hebbian-like correction)**, т.е. подстраивает веса так, чтобы нейрон, активированный SOM, “голосовал” за правильный класс.

📊 Итог:

* SOM строит карту признаков (самоорганизация).
* Grossberg Layer связывает нейроны карты с метками классов.
* Получается гибрид “кластеризации + классификации”.

---

### ⚙️ Как идёт обучение (по шагам)

1. SOM проходит через все данные и подстраивает веса нейронов под структуру входов.
2. Для каждого входа находится **BMU (Best Matching Unit)** — нейрон-победитель.
3. Grossberg Layer принимает индексы этих BMU как входы и учится предсказывать правильные метки классов.
4. В ходе обучения Grossberg Layer корректирует свои веса на основе ошибки между целевым one-hot вектором и текущими весами.

---

### 💡 Какой тип функции используется в слое Гроссберга

В GrossbergLayer веса обновляются по правилу:

```python
self.weights[bmu] += self.lr * (y_true - self.weights[bmu])
```

Это **линейное обучение с экспоненциальным сглаживанием** — разновидность **адаптивного фильтра Гроссберга**, без нелинейной активации.
Тип связи — **ассоциативная (Hebbian-like)**, где веса представляют вероятностные ассоциации “нейрон карты → класс”.

---

## 🔍 Подробное объяснение части, о которой нужно рассказать стажёрам

---

### 🧩 Класс `GrossbergLayer`

```python
class GrossbergLayer:
    def __init__(self, num_neurons, num_classes, lr=0.01, n_iter=50):
        self.weights = np.random.rand(num_neurons, num_classes)
        self.lr = lr
        self.n_iter = n_iter
```

**Что происходит:**

* `num_neurons` — количество нейронов на SOM (входов).
* `num_classes` — количество классов (выходов).
* Матрица весов размером `[нейроны SOM × классы]` хранит, насколько каждый нейрон связан с каждым классом.
* `lr` — скорость обучения, `n_iter` — число эпох.

---

#### Обучение

```python
def fit(self, bmu_indices, labels):
    Y = np.eye(self.weights.shape[1])[labels]
    for _ in range(self.n_iter):
        idx = np.random.permutation(len(bmu_indices))
        for i in idx:
            bmu = bmu_indices[i]
            y_true = Y[i]
            self.weights[bmu] += self.lr * (y_true - self.weights[bmu])
```

**Объяснение:**

* `bmu_indices` — индексы нейронов-победителей из SOM.
* `Y` — one-hot представление правильных меток.
* Для каждого примера выбирается нейрон-победитель (`bmu`).
* Его веса обновляются по направлению к правильному классу.
* Это **простое линейное ассоциативное обучение**, без обратного распространения ошибки.

---

#### Предсказание

```python
def predict(self, bmu_indices):
    return np.argmax(self.weights[bmu_indices], axis=1)
```

**Смысл:**
Для каждого BMU выбирается класс с максимальным весом.
То есть нейрон “голосует” за тот класс, с которым у него сильнее всего связь.

---

### 🧠 Класс `CPN`

Он объединяет две части — SOM и GrossbergLayer.

* Сначала SOM самоорганизуется.
* Потом GrossbergLayer обучается предсказывать метку по победителю SOM.
* В ходе обучения CPN считает accuracy на train и test, чтобы построить график.

---

## 📈 Блоки визуализации

---

### **Блок 8 — График точности**

```python
plt.figure(figsize=(8,5))
plt.plot(cpn.train_acc_history, label='Train acc', color='purple')
plt.plot(cpn.test_acc_history, label='Val acc', color='cyan')
plt.xlabel('Epoch')
plt.ylabel('Acc')
plt.title('Acc CPN')
plt.legend()
plt.grid(True)
plt.show()
```

**Что он показывает:**

* По оси X — эпохи обучения слоя Гроссберга.
* По оси Y — точность классификации (accuracy).
* Две линии: обучение и валидация.
* Можно видеть, как сеть стабилизируется и достигает насыщения.
* Стажёрам важно отметить, что колебания — это нормальное явление при стохастическом обновлении.

---

### **Блок 9 — Визуализация SOM через PCA**

**Идея:** показать, как веса SOM (нейроны) расположены в пространстве признаков относительно исходных данных.

```python
pca = PCA(n_components=2)
X2 = pca.fit_transform(X_train)
weights2 = pca.transform(cpn.kohonen.weights)
```

* Сначала данные и веса проецируются в 2D (через PCA).
* Точки — обучающие данные, окрашенные по классам.
* Красные кресты — нейроны SOM (центры кластеров).
* Серые линии соединяют соседей на сетке, показывая топологию карты.

📘 Это классическая **визуализация самоорганизующейся карты**:
чем плотнее нейроны — тем плотнее данные; чем ярче границы — тем больше различие между классами.

---

### **Блок 10 — U-Matrix (матрица расстояний)**

```python
plot_u_matrix(cpn.kohonen)
```

**Что такое U-Matrix:**
Это тепловая карта средних расстояний между соседними нейронами SOM.

* Малые значения → области с похожими векторами (однородные кластеры).
* Большие значения → границы между классами.

Это помогает увидеть, где SOM “разделила” данные на кластеры.
U-Matrix — стандартный инструмент анализа качества самоорганизации.

---

## 🧩 Итого для стажёров:

| Компонент                     | Роль                              | Тип обучения     | Ключевая идея                                                    |
| ----------------------------- | --------------------------------- | ---------------- | ---------------------------------------------------------------- |
| **SOM (Кохонен)**             | Извлекает структуру данных        | Неподконтрольное | Нейроны самоорганизуются в топологическую карту признаков        |
| **Grossberg Layer**           | Ассоциирует нейроны с классами    | Подконтрольное   | Обучение на основе ошибки между предсказанным и реальным классом |
| **CPN**                       | Объединяет оба слоя               | Комбинированное  | “Кластеризация + классификация”                                  |
| **Функция в слое Гроссберга** | Линейное ассоциативное обновление | Hebbian-like     | Δw = lr × ( target – output )                                    |
| **U-Matrix**                  | Визуализация структуры SOM        | Аналитическая    | Показывает границы между кластерами                              |


